{
  "name": "claude-mermaid",
  "private": true,
  "description": "Render mermaid diagrams in the Claude Code terminal",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && bun build src/mermaid_display.js --outdir dist --target node --conditions bun --format esm --splitting --minify",
    "test": "bun run build && bun test"
  },
  "dependencies": {
    "beautiful-mermaid": "^1.1.3"
  },
  "patchedDependencies": {
    "beautiful-mermaid@1.1.3": "patches/beautiful-mermaid@1.1.3.patch"
  }
}
